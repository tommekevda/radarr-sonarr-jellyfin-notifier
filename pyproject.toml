[project]
name = "radarr-sonarr-jellyfin-notifier"
version = "1.1.1"
description = "Webhook listener for Radarr or Sonarr that triggers a Jellyfin refresh"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["flask>=3.1.1", "requests>=2.32.3"]
