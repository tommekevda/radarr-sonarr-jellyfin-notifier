[project]
name = "radarr-sonarr-jellyfin-notifier"
version = "0.2.1"
description = "Webhook listener for Radarr or Sonarr that triggers a Jellyfin library refresh"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["flask>=3.1.1", "requests>=2.32.3"]
